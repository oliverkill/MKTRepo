import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: 180,
    },
    image: {
        width: '100%',
        height: 180,
    },
    text: {
        color: 'grey',
        marginTop: 10,
    },
})

export default styles;